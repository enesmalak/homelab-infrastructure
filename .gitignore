# Secrets — never commit real values (only *.example templates are tracked)
*.env
!*.env.example
pve.yml

# Runtime data / bind-mount targets created by the containers
data/
config/
cache/
metadata/
influxdb/
mongo-data/
model-cache/
reports/
campaigns/
portraits/
