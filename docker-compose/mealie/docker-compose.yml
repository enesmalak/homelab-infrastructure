services:
  mealie:
    image: ghcr.io/mealie-recipes/mealie:latest
    container_name: mealie
    restart: unless-stopped
    ports:
      - "9925:9000"
    volumes:
      - ./data:/app/data/
    environment:
      - TZ=Europe/Berlin
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
