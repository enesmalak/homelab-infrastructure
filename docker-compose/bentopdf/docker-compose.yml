services:
  bentopdf:
    # Using the 'simple' image for a lightweight, stateless deployment
    image: bentopdf/bentopdf-simple:latest
    container_name: bentopdf
    restart: unless-stopped
    ports:
      - "3000:8080"
